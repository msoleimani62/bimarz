// نقطه‌ورود باینری کمکی uniffi-bindgen — فقط روی ماشین توسعه اجرا می‌شود.
// Entry point for the uniffi-bindgen helper binary — runs on the
// development machine only.
fn main() {
    uniffi::uniffi_bindgen_main()
}

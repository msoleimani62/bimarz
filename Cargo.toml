[workspace]
resolver = "2"
members = ["engine-core"]

# Shared release profile for all workspace crates.
# پروفایل انتشار مشترک برای تمام کریت‌های workspace.
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "unwind"
